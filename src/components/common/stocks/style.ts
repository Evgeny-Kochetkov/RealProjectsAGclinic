import styled from 'styled-components'

export const SImgWr = styled.div`
    position: relative;
    border-radius: 10px;
    overflow: hidden;
`

export const SSale = styled.div`
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 0 15px;
    min-width: 69px;
    height: 56px;
    border-radius: 0 40px 40px 40px;
    background: var(--Red, #EB5757);
    color: #FFF;
    text-align: center;
    font-family: var(--font-cambria);
    font-size: 20px;
    font-style: normal;
    font-weight: 700;
    line-height: normal;
    position: absolute;
    z-index: 1;
    top: -2px;
    left: 0;
`